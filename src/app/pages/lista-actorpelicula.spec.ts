import { ListaActorpelicula } from './lista-actorpelicula';

describe('ListaActorpelicula', () => {
  it('should create an instance', () => {
    expect(new ListaActorpelicula()).toBeTruthy();
  });
});
